Start the demo dev server for local development.

## Steps

1. Run the demo dev server:
   ```
   npm run dev:demo
   ```

2. Tell the user the dev server is starting and they can open the URL shown in the terminal output.
