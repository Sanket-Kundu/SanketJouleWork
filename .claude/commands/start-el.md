Start the el-demo dev server for local development.

## Steps

1. Run the el-demo dev server:
   ```
   npm run dev:el-demo
   ```

2. Tell the user the dev server is starting and they can open the URL shown in the terminal output.
